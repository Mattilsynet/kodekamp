{
  "dependencies": {
    "fastify": "^5.6.1",
    "node": "^25.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node index.js",
    "test": "npm run build && npm run start"
  }
}
